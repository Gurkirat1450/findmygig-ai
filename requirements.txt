fastapi==0.115.9  # bumped from 0.115.0 — crewai==0.86.0 pulls in chromadb,
                  # which pins fastapi==0.115.9; the two must match.
uvicorn[standard]==0.30.6
pydantic==2.13.4
python-dotenv==1.0.1
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
sentence-transformers==3.1.1
faiss-cpu==1.9.0
google-generativeai==0.8.3
langchain==1.3.17
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-google-genai==4.3.5
langgraph==1.2.11
crewai==0.86.0
setuptools<81   # crewai 0.86.0's telemetry module does `import pkg_resources`,
                # which setuptools>=81 removed (PEP 632) — pin below that.
litellm==1.100.0 # bumped from 1.53.9 — that version pinned httpx<0.28, which now
                 # conflicts with google-genai>=1.65.0 (pulled in by
                 # langchain-google-genai==4.3.5), which needs httpx>=0.28.1.
                 # NOTE: litellm==1.100.0 does `from typing import NotRequired`,
                 # which needs Python 3.11+ (the Dockerfile's python:3.11-slim
                 # has it; a Python 3.10 local venv will fail to import litellm —
                 # recreate the local venv on 3.11 to match).
httpx==0.28.1    # bumped from 0.27.2 to satisfy google-genai>=1.65.0's httpx>=0.28.1
                 # floor; litellm==1.100.0 no longer caps httpx below 0.28.
scikit-learn

# Coming in Week 2:
# autogen
